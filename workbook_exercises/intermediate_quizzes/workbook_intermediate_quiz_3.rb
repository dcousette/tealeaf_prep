#workbook_intermediate_quiz_3

3

# My string looks like this now: pumpkins
# My array looks like this now: ["pumpkins", "rutabaga"]

#the string operation += creates a new string object, while the "mystring" variable
#still points to the same value prior to the method call

#the << operation appends to the array object and still points to the same 
#object even from inside the method.

4

# My string looks like this now: rutabaga
# My array looks like this now: ["pumpkins"]

5

  def color_valid
    color == "blue" || color == "green 
  end
  